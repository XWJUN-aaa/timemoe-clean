# Time-MoE 模型使用说明

## 当前状态

✅ **已完成：**
- Conda 虚拟环境 `timemoe` 已创建
- Python 3.10 和所有依赖包已安装
- 模型配置文件已下载
- 测试脚本已准备就绪

⚠️ **待完成：**
- 模型权重文件需要下载（约 226 MB）

## 模型文件下载方法

### 方法 1：使用 huggingface-cli 下载（推荐）

如果网络可用，在 conda 环境中运行：

```bash
# 激活环境
source /root/miniconda3/etc/profile.d/conda.sh
conda activate timemoe

# 安装 huggingface-hub（已安装）
# 下载模型
huggingface-cli download Maple728/TimeMoE-50M \
    --local-dir /root/projects/timemoe/TimeMoE-50M \
    --local-dir-use-symlinks False
```

### 方法 2：手动下载

1. 在有网络的机器上访问：https://huggingface.co/Maple728/TimeMoE-50M
2. 下载 `model.safetensors` 文件（226 MB）
3. 将文件传输到服务器并替换：
   ```bash
   # 替换现有的指针文件
   cp /path/to/downloaded/model.safetensors /root/projects/timemoe/TimeMoE-50M/model.safetensors
   ```

### 方法 3：使用镜像站点

```bash
cd /root/projects/timemoe/TimeMoE-50M
# 下载真实的模型文件
wget https://hf-mirror.com/Maple728/TimeMoE-50M/resolve/main/model.safetensors -O model.safetensors
```

## 运行模型

### 1. 激活虚拟环境

```bash
cd /root/projects/timemoe
source /root/miniconda3/etc/profile.d/conda.sh
conda activate timemoe
```

### 2. 运行测试脚本

**方法 A：使用提供的 shell 脚本**
```bash
bash run_timemoe.sh
```

**方法 B：直接运行 Python 脚本**
```bash
python test_timemoe_mirror.py
```

## NPU 支持

当前虚拟环境中未安装 `torch_npu`。如果需要使用 NPU：

```bash
# 激活环境
conda activate timemoe

# 安装 torch_npu（需要根据您的 NPU 硬件版本安装相应版本）
# 例如：
pip install torch-npu
```

## 文件说明

- `test_timemoe_mirror.py` - 模型测试脚本（支持 NPU/CPU 自动切换）
- `run_timemoe.sh` - 便捷启动脚本
- `TimeMoE-50M/` - 模型文件目录
- `requirements.txt` - Python 依赖列表

## 当前环境状态检查

```bash
# 激活环境
conda activate timemoe

# 检查 Python 版本
python --version

# 检查已安装的包
pip list | grep -E "(torch|transformers|datasets)"

# 检查模型文件大小
ls -lh TimeMoE-50M/model.safetensors
```

## 验证模型文件

模型文件正确下载后应该：
- 文件大小：约 216 MB (226,760,264 字节)
- SHA256：0127209833663df6f5ae3cf1c3316f739a8dd1dae27e59036268bbfdb48f91a4

检查命令：
```bash
ls -lh /root/projects/timemoe/TimeMoE-50M/model.safetensors
sha256sum /root/projects/timemoe/TimeMoE-50M/model.safetensors
```

## 故障排除

### 问题 1：网络无法访问 Hugging Face
- 使用镜像站点：https://hf-mirror.com
- 或手动下载后传输文件

### 问题 2：模型文件只有 134 字节
- 这是 Git LFS 指针文件，需要下载真实文件
- 参考上述下载方法

### 问题 3：NPU 不可用
- 检查是否是无卡开机状态
- 安装 torch_npu
- 或使用 CPU 模式运行（脚本会自动切换）

## 联系与支持

- Time-MoE 项目：https://github.com/Time-MoE/Time-MoE
- Hugging Face 模型：https://huggingface.co/Maple728/TimeMoE-50M


